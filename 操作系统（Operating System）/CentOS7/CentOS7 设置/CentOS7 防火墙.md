# CentOS7 防火墙

```shell
sudo systemctl status firewalld     # 查看防火墙的状态
sudo systemctl start firewalld # 临时打开防火墙，立即生效。
sudo systemctl stop firewalld      # 临时关闭防火墙，立即生效。
sudo systemctl enable firewalld  # 永久开启防火墙，重启后生效。
sudo systemctl disable firewalld # 永久关闭防火墙，重启后生效。
```

‍
